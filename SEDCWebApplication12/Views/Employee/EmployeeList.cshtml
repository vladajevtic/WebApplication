@model IEnumerable<Employee>


<h1>@ViewBag.Title</h1>
<div>
    <ul>
        @foreach ( var employee in Model)
        {
        <li>@employee.Name, @employee.Company <a href="/Employee/EmployeeDetails/@employee.Id">Details</a></li>
        }
    </ul>
</div>